{
  "name": "rate-converter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "ensure:port": "node scripts/ensure-port.mjs",
    "dev": "node scripts/start-service.mjs dev",
    "build": "vite build",
    "preview": "node scripts/start-service.mjs prod",
    "start": "npm run build && node scripts/start-service.mjs prod",
    "test:port": "node --test scripts/test-fixed-port.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
